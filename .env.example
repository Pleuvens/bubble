# Bubble Environment Configuration
# Copy this file to .env and configure with your values

# =============================================================================
# DEVELOPMENT CONFIGURATION
# =============================================================================

# Database Configuration (Development)
# Default PostgreSQL connection for local development
# DATABASE_URL=ecto://postgres:postgres@localhost/bubble_dev

# Server Configuration (Development)
# PHX_HOST=localhost
# PORT=4000

# =============================================================================
# PRODUCTION CONFIGURATION
# =============================================================================

# Database Configuration (Production)
# PostgreSQL connection string - REQUIRED in production
# Format: ecto://USER:PASS@HOST/DATABASE
# Example: ecto://bubble_user:securepassword@db.example.com/bubble_prod
# DATABASE_URL=

# Secret Key Base (Production)
# Used to sign/encrypt cookies and other secrets - REQUIRED in production
# Generate with: mix phx.gen.secret
# SECRET_KEY_BASE=

# Application Host (Production)
# Your production domain name - REQUIRED in production
# PHX_HOST=example.com

# Server Port (Production)
# Port the application will listen on
# PORT=4000

# Connection Pool Size
# Number of database connections in the pool (default: 10)
# POOL_SIZE=10

# IPv6 Support
# Set to "true" or "1" to enable IPv6
# ECTO_IPV6=false

# DNS Cluster Query
# For distributed Elixir nodes
# DNS_CLUSTER_QUERY=

# WebSocket Origin Checking (Production)
# Controls which origins are allowed to connect via WebSocket (LiveView)
# Options:
#   - false: Allow all origins (most flexible for self-hosting, less secure)
#   - comma-separated list: https://yourdomain.com,https://www.yourdomain.com
#   - not set: defaults to PHX_HOST (most secure, single domain)
# For self-hosted deployments, set to "false" or list your domains
# CHECK_ORIGIN=false

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================

# Phoenix Server
# Set to "true" to automatically start the server (useful for releases)
# PHX_SERVER=false

# =============================================================================
# API KEYS & EXTERNAL SERVICES
# =============================================================================

# Add any API keys for external services here
# Note: Never commit actual API keys to version control

# Example: Email service API keys
# MAILGUN_API_KEY=
# MAILGUN_DOMAIN=

# Example: Analytics or monitoring services
# SENTRY_DSN=
# GOOGLE_ANALYTICS_ID=

# =============================================================================
# NOTES
# =============================================================================
# - Never commit this file with actual secrets to version control
# - Use different values for development, staging, and production
# - In production, set these as environment variables or use a secrets manager
# - Generate secure random values for SECRET_KEY_BASE (min 64 characters)
